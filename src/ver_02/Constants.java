package ver_02;

import java.io.File;

public class Constants {
  public static final String WEB_ROOT = System.getProperty("user.dir") + File.separator  + "webroot";

  public static final String SERVLET_ROOT = System.getProperty("user.dir") + File.separator  + "out"
          + File.separator + "servlet";
}